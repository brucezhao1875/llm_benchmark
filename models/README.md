# Model Benchmarks

Each subdirectory contains benchmark material for one model and hardware/runtime combination.

Naming convention:

```text
<model-name>-<hardware-or-platform>
```

Example:

```text
glm-5.2-fp8-h20
```
